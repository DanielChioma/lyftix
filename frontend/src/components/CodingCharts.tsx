import {Bar,CartesianGrid,ComposedChart,Legend,Line,ResponsiveContainer,Tooltip,XAxis,YAxis} from'recharts';import type{CodingAnalyticsResponse}from'../api/analytics.types';import{formatChartDate}from'../utils/format';import{secondsToHours,sortCodingDaily}from'../utils/coding';
export function CodingCharts({analytics}:{analytics:CodingAnalyticsResponse}){const data=sortCodingDaily(analytics.daily).map(d=>({...d,durationHours:secondsToHours(d.durationSeconds)}));return <div className="charts-grid"><section className="chart-card"><div className="chart-heading"><h2>Daily coding activity</h2><p>Coding duration in hours (left axis) and session count (right axis).</p></div>{data.length===0?<p className="section-empty">No coding sessions were recorded in this range.</p>:<div className="chart-container"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date" tickFormatter={formatChartDate}/><YAxis yAxisId="hours" label={{value:'Hours',angle:-90,position:'insideLeft'}}/><YAxis yAxisId="count" orientation="right" allowDecimals={false} label={{value:'Sessions',angle:90,position:'insideRight'}}/><Tooltip labelFormatter={v=>formatChartDate(String(v))}/><Legend/><Bar yAxisId="hours" dataKey="durationHours" name="Coding hours" fill="var(--chart-blue)"/><Line yAxisId="count" dataKey="sessionCount" name="Sessions" stroke="var(--chart-green)" strokeWidth={2}/></ComposedChart></ResponsiveContainer></div>}</section></div>}
